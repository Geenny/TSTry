import Dependency from '../framework/core/dependency/Dependency';
export default class UserDependency extends Dependency {
    constructor(ID, dependencies = []) {
        super(ID, dependencies);
    }
    start() {
        super.start();
    }
    finish() {
        super.finish();
    }
}
//# sourceMappingURL=UserDependency.js.map